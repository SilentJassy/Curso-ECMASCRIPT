let hello = 'Hello'
let world = 'World'
let epicPhrase = hello + ' ' + world + '!'

console.log(epicPhrase)

// Template literals

let epicPhrase2 = `${hello} ${world}!!`

console.log(epicPhrase2)

// Multi-lines strings

let lorem = 'Esto es un string \n' + 'Esta es otra linea'

let lorem2 = `Esto es una frase epica,
la continuacion de esa misma frase.
`

console.log(lorem)
console.log(lorem2)